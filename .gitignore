
# Gate 3 staging residue (deploy-gridnode.sh mutation backups, 2026-08-27)
deploy-gridnode.sh.bak-*

# Wrangler runtime cache (created by wrangler login/deploy)
_deploy_v1.3/.wrangler/

# Wrangler runtime cache (device flow, login state)
.wrangler/

# Generated staging output: copy of baseline written by deploy-gridnode.sh Step 1
_deploy_v1.3/index.html

# Generated staging output: copy of baseline written by deploy-gridnode.sh Step 1
_deploy_v1.3/index.html

# Dependencies (project-local — installed via npm in workspace root)
node_modules/

# Windows path leak (accidental mkdir from WSL/Windows crossover)
C:/

# Codex-generated images (not source assets)
Codex Image *.png

# Staging directories (build output, not source)
.staging/
