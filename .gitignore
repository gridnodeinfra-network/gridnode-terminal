.staging/
backups/
.gridnode-secrets/
.wrangler/
node_modules/

# Playwright user-data-dir misresolves to ./undefined: — never commit
undefined:*

# Accidental Lighthouse profile paths (never track)
**/lighthouse.*/
**/undefined*/
.worktrees/
__pycache__/

# ACCESS//GATE dev fixtures (local-only state)
dev/.dev-state.json
